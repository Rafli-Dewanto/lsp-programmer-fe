export interface UpdateEmployeePayload {
  name: string;
  email: string;
  address: string;
  role: string;
}

export interface Employee {
  id: number;
  name: string;
  email: string;
  address: string;
  role: string;
}
